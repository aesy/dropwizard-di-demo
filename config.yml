server:
    applicationConnectors:
      - type: http
        port: 8080

database:
    driverClass: org.postgresql.Driver
    url: jdbc:postgresql://localhost/test
    user: postgres
    password: postgres

logging:
    level: INFO
